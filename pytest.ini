[pytest]
addopts = -ra
# examples/ holds scripts that run against a live installation, never part of the suite.
norecursedirs = examples .git node_modules
markers =
    integration: requires CKAN's PostgreSQL test database and plugin fixtures
